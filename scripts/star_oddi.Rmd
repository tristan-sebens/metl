```{r}
library(tidyverse)

so_d = here::here('data', 'Star Oddi')
so_dst_d = file.path(so_d, 'DST', 'Sablefish', 'JS7977')
so_dstmag_d = file.path(so_d, 'DST magnetic', 'Sablefish', 'JS927')
so_dstmf_d = file.path(so_d, 'DST milli-F', 'Sablefish', 'JS122')
```


DST 
```{r}
dc_so.dst__ = Decoder_StarOddi_DST(d = so_dst_d)

dc_so.dst__$decode(con)
```
DST Magnetic
```{r}
dc_so.dstmag__ = Decoder_StarOddi_DSTmagnetic(d = so_dstmag_d)

dc_so.dstmag__$decode(con)
```

DST milli-F 
```{r}

```

































